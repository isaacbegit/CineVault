<Window x:Class="CineVault.Views.TrailerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Trailer" Height="620" Width="940" MinWidth="520" MinHeight="380"
        Style="{StaticResource ThemedWindowStyle}"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TitleText" Grid.Column="0" Text="Trailer" FontSize="18" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />

            <Button Grid.Column="1" Content="✕" Style="{StaticResource IconButton}" Click="Close_Click" />
        </Grid>

        <Border Grid.Row="1" CornerRadius="12" ClipToBounds="True" Background="Black">
            <wv2:WebView2 x:Name="TrailerWebView" />
        </Border>
    </Grid>
</Window>
